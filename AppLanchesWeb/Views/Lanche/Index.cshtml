@model IEnumerable<AppLanchesWeb.Models.Lanche>

<div><h2>Todos os Lanches</h2></div>


@foreach(var lanche in Model)
{
    <div>
        <h4>Lanche : @lanche.Nome</h4>
        <p><img src="@lanche.ImagemUrl"/></p>
        <p>Preço: @lanche.Preco.ToString("c")</p>
    </div>
}